Building out an "Arcade" section for Homegrown Hoops. I'm attaching four
files: avatar-creator-prototype.html, beat-the-d-prototype.html,
shot-clock-prototype.html, and HigherLowerDemo.jsx.

FIRST: I started this build in an earlier session that ran out of credits
partway through. Please check the current state of the codebase before
doing anything — figure out which of the steps below are already done,
partially done, or not started, and pick up from there rather than
restarting. Tell me what you find before proceeding if anything is unclear.

IMPORTANT CROSS-CUTTING RULE: Every single game (Fast Break!, Who Ya
Got?, Shot Clock) must save that player's results when a session ends,
and those results must show up on the player's profile/card page. This
isn't optional for any one game — Step 3 sets up the shared system, and
each game's step below says exactly what to send it.

Then work through these steps in order — each depends on the one before it.

STEP 1 — Avatar Creator
Build the avatar creator: players customize a 2D cartoon avatar (skin
tone, hair, build, jersey color/style, accessories, expression — see
avatar-creator-prototype.html for the full customization set and the
exact character-rendering approach) and can choose to use it in place of
a photo on their player card. Store the avatar config as JSON per player.
Check the current player card page/component first so this fits existing
conventions.

STEP 2 — Arcade nav + hub page
Add "Arcade" to the site's dropdown navigation. It links to a new Arcade
page listing our mini-games as tiles: "Fast Break!", "Who Ya Got? (NBA
Higher or Lower)", and "Shot Clock". Each tile launches its game.

STEP 3 — Stats storage (shared across all 3 games) — REQUIRED for every game
Add per-player storage for mini-game stats: high score, fgm/fga, tpm/tpa
(to compute FG%/3PT%), best streak, and highest level reached (Fast
Break!-specific — Who Ya Got? uses best streak, Shot Clock uses best
score/best streak). Add an API endpoint each game calls at the end of a
session to submit that session's results; only overwrite "best" values if
beaten, always add to career totals where relevant. Add a stats section
to the player card/profile page showing these for all three games.

STEP 4 — "Fast Break!" (the platformer)
Port in beat-the-d-prototype.html — it's a complete, working Phaser game.
Rename all player-facing "Beat the D" references to "Fast Break!". Wire
it to: (a) use the player's avatar from Step 1 as the in-game sprite
instead of the random "Choose Your Hooper" screen, falling back to that
random-pick screen if no avatar exists yet, (b) call the Step 3 API at
session end (score, fgm/fga, tpm/tpa, best streak, level reached) — this
is required, not optional.

STEP 5 — "Who Ya Got?" (NBA Higher or Lower — stat comparison game)
Port in HigherLowerDemo.jsx — it's a complete, working React game.
Rename all player-facing references to "Who Ya Got?". Swap the
placeholder PlayerAvatar component for our real avatar system from Step
1 (fall back to initials if a player has no avatar yet). Wire it to the
Step 3 stats API (best streak) at session end — this is required, not
optional. The player dataset is hardcoded in the file for now — leave
as-is unless we already have a better NBA stats source wired up elsewhere.

STEP 6 — "Shot Clock" (reaction/timing game)
Port in shot-clock-prototype.html — it's a complete, working game (plain
HTML/CSS/JS, no external dependencies). Rename any generic references to
fit our branding if needed. There's a marked hand-off point in the file's
JS (search "Hand-off point") showing exactly where to call the Step 3
stats API at session end (score, best streak) — this is required, not
optional.

Check the current codebase structure before starting each step so
everything matches existing patterns.
